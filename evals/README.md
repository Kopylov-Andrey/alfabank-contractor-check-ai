# Evaluation artifacts

This directory is reserved for immutable evaluation metadata and sanitised run
exports. The canonical test definitions currently live in
`apps/regression-dashboard/app/data/test_cases.json` and are based on
`docs/04_ai_evaluation_test_cases.md`.

Do not overwrite a frozen run after evaluation has started.

