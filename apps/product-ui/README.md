# Product UI

Place the end-user interface here when its current implementation is ready for
the repository. Do not commit API keys into frontend code. Calls to Yandex
Responses API must go through a backend.

